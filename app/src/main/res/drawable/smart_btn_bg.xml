<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <corners android:radius="5dp" />
    <!-- 圆角的半径  也可以统一设置半径 android:radius="9dip" -->
    <solid android:color="@color/smart_btn_bg" />

    <stroke
        android:width="0.1dp"
        android:color="@color/smart_bt_bg_line" />

    <padding
        android:bottom="@dimen/dim24"
        android:left="@dimen/dim24"
        android:right="@dimen/dim24"
        android:top="@dimen/dim24" />

</shape>